import pkg from "pg"; const { Client } = pkg; const client = new Client({connectionString: process.env.DATABASE_URL, ssl: {rejectUnauthorized: false}}); client.connect().then(() => {console.log("✅ DB Connected"); return client.query("SELECT COUNT(*) FROM questions WHERE category IN ('music', 'movies')");}).then(r => {console.log("Current counts:", r.rows); client.end();}).catch(e => {console.error("❌ Error:", e.message); client.end();});
